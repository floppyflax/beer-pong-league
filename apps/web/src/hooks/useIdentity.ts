/**
 * Re-export — canonical impl now lives in `@elofight/shared/hooks/useIdentity`.
 */
export { useIdentity } from '@elofight/shared/hooks/useIdentity';
export type { IdentityState } from '@elofight/shared/hooks/useIdentity';
