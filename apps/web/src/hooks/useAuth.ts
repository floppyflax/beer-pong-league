/**
 * Re-export — canonical impl now lives in `@elofight/shared/hooks/useAuth`.
 */
export { useAuth } from '@elofight/shared/hooks/useAuth';
export type { AuthState } from '@elofight/shared/hooks/useAuth';
