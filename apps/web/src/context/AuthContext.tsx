/**
 * Re-export — canonical impl now lives in `@elofight/shared/contexts/AuthContext`.
 */
export { AuthContext, AuthProvider, useAuthContext } from '@elofight/shared/contexts/AuthContext';
export type { AuthContextType } from '@elofight/shared/contexts/AuthContext';
